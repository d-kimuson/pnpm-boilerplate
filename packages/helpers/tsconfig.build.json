{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.src.json",
  "include": ["src"],
  "compilerOptions": {
    "composite": false // tsup で dts の書き出しができないので
  },
  "exclude": ["node_modules", "dist", "src/**/*.test.ts"]
}
